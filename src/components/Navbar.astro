---
const items = [
    {
        name: "Home",
        href: "/",
    },
];
---

<nav>
    {
        items.map((item, idx) => (
            <>
                <a href={item.href}>{item.name}</a>
                {idx < items.length - 1 && " / "}
            </>
        ))
    }
</nav>
